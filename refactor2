☐ Refactor SettingsRowWindow layout engine (no functional changes)
Phase 1 — Remove type knowledge from the layout engine
x Introduce generic layout structures (LayoutItem, LayoutColumn).
x Refactor computeRowLayout() to receive generic columns instead of bool isSlider.
x Verify Graphics and Audio screens render exactly the same.
Phase 2 — Build columns at the call site
☐ Create helper for the label column.
☐ Create helper for a setting-value column.
☐ Create helper for a slider column.
☐ Change render() to construct columns before calling computeRowLayout().
☐ Remove all slider-specific logic from computeRowLayout().
Phase 3 — Remove duplicated button layout
x Delete computeButtonRowBox().
x Build a button row using the same generic layout engine.
x Verify Back button width matches every other row exactly.
Phase 4 — Clean up layout API
☐ Remove obsolete parameters (sharedControlsWidth, valueText, etc.) from computeRowLayout() if no longer needed.
☐ Rename anything that no longer reflects its purpose.
☐ Verify no duplicate layout calculations remain.
Phase 5 — Final polish
☐ Review helpers for naming consistency.
☐ Ensure every row type (Button, Setting, Slider) is created by composing columns, not by special-casing inside the layout engine.
☐ Confirm there are no behavioral or visual regressions.