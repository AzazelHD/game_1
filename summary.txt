TRPG PROJECT — UNIFIED SUMMARY (unitsummary)

=== SETTINGS MENU UNIFICATION — SettingsRowWindow + engine IFocusable integration ===
[x] All items complete and merged to master (see GIT WORKFLOW below).
[x] ConfirmWindow default-focus fix (Discard/Cancel default, not Confirm/Apply) — applied and verified via FocusGroup trace.
[x] SettingsRowWindow Phase 1–5 layout refactor — confirmed in-game, renders identically.
[x] Settings "Graphics" page — explicitly stays as-is (Resolution + Window Mode only). No Video/Graphics split. Closed, not deferred.

=== CLASS RENAME ===
[x] ActionMenuWindow → ButtonMenuWindow — project-wide sweep confirmed complete via findstr, zero remaining references.

=== GIT WORKFLOW ===
[x] refactor/settings-split merged into master (fast-forward, confirmed via git log), pushed to origin. Nothing outstanding on this branch.

=== SDL DECOUPLING (this arc) ===
[x] App::requestQuit() added (App.h/.cpp) — game code no longer touches SDL directly for quit.
[x] WorldMapState.cpp, MainMenuState.cpp — SDL_Event/SDL_PushEvent replaced with App::requestQuit().
[x] Dead #include <SDL3/SDL.h> removed from BattleMap.cpp (was unused).
[x] Confirmed remaining SDL includes are expected/correct: main.cpp (entry point), App.cpp/Window.cpp (owning wrappers), vcpkg third-party headers (not project code).

=== PARTYWINDOW REBUILD ===
[x] Reviewed — was clean (no pre-WindowId/ActionId remnants), but hand-rolled nav instead of FocusGroup.
[x] Rebuilt on FocusGroup/IFocusable (EntryFocusable wrapper), replacing manual m_selected/m_scroll toggling. Note: FocusGroup wraps around (vs. old clamp-at-ends behavior) — accepted as consistent with ConfirmWindow/SettingsRowWindow.
[x] Accept now distinct from Back: emits ActionSelected/Inspect(index) instead of duplicating Close.
[x] Row highlight now uses UIUtils::formatButtonLabel instead of hand-rolled "> label <" formatting.
[x] ui/windowId.h include-casing bug fixed (→ WindowId.h, matches project convention).

=== UnitInspectWindow (new class) ===
[x] Built as the permanent replacement for the old generic InspectWindow — curated fields only (name, job, Stats section), colored border by Relation (Player=blue, Ally=green, Enemy=red), explicitly scoped to be extended with more Sections later (Skills/Gear) once the unit progression system lands. Not a temporary hack.
[x] Wired into WorldMapState (PartyWindow's Accept → node info) and BattleState's DeploymentWindow's Details/Tab flow.
[x] Wired into BattleMenuController (showInspectWindow, showInspectWindowFromTemplate) — the deferred BattleState swap is DONE. Old InspectWindow.h/.cpp confirmed fully dead via findstr (only self-reference remained) and deleted.
[x] WindowId::PartyInspect added.

=== WORLD MAP ===
[x] Tab now opens node info (DialogWindow reused, "Empty Node" default description on all nodes). Party still reachable via Esc menu, per explicit decision to keep both.
[x] NodeMeta::description field added (defaults "Empty Node").

=== BUG FIXES (this arc) ===
[x] Boss/critical-unit death not triggering defeat — test_map's BattleDefinition was missing .defeatRule = BattleDefeatRule{.type = DefeatConditionType::CriticalUnitDied}. One-line BattleCatalog.cpp fix. Confirmed working in-game.
[x] Move/Attack Accept doing nothing in combat — HumanTurnController::handleActiveTurn had MoveTarget/AttackTarget branches nested unreachably inside `if (phase == FreeCursor)` as dead else-ifs. Fixed to sibling else-ifs off the same top-level phase check. Confirmed working in-game.
[x] Deployment initial selection landing on an already-placed forced/boss unit (e.g. Aria) instead of skipping to the first unplaced roster slot — DeploymentSystem::initialize() now applies the same "first unplaced in roster order" rule placeGrabbed() already used, once at startup. Confirmed working in-game.
[x] Confirmed pre-existing and correct, no fix needed: forced/boss units' .locked flag already prevented grab/unplace (grabUnit/unplaceUnit both check it); maxUnits was already correctly sourced from BattleDefinition, not a second source of truth.

=== FOLDER RESTRUCTURE ===
[x] states/ → scenes/ (renamed to match actual base class Scene; states/ was ambiguous with "game state" data).
[x] HumanTurnController, AttackResolutionController, DeploymentPhaseController, BattleMenuController → battle/ (domain/orchestration logic, not Scene subclasses).
[x] WorldMapState → scenes/ (was inconsistently in its own world/ folder; world/ now holds only WorldGraph.h/WorldPathfinding.h, pure data structures).
[x] All #include paths and the .vcxproj updated accordingly. Confirmed building clean.

=== BATTLESTATE SRP / GOD-CLASS BREAKUP — all four seams complete ===
Seam #1 — AttackResolutionController (battle/):
[x] Owns PendingAttackController, pending skill id, roll-ahead hit-result playback (prepare/cycleFocus/updatePreview/beginResolution/processNextResult/cancel).
[x] BattleState::AttackResolutionContext bundles the shared turn-flow state it needs (mirrors HumanTurnContext pattern). TurnState/PendingResolution enums made public on BattleState for this.

Seam #2 — DeploymentPhaseController (battle/):
[x] Owns DeploymentSystem outright (not just orchestration around it) plus the preview Unit* list and DeploymentWindow pointer.
[x] BattleState::DeploymentContext bundles shared state. BattleFlowPhase enum made public for this.
[x] handleUIEvent() replaces the old inline WindowId::BattleDeployment block in BattleState::processUIEvents.

Seam #3 — BattleMenuController (battle/):
[x] Owns the action menu, skill submenu, system menu, and the full unit-inspect flow (inspect menu + UnitInspectWindow itself — see above).
[x] BattleState::MenuContext bundles shared state (largest context struct, includes references to AttackResolutionController/DeploymentPhaseController for cross-controller calls like Start Battle confirm and move-undo).
[x] Deferred InspectWindow→UnitInspectWindow swap folded in here — see UnitInspectWindow section above.

Seam #4 — render() decomposition (no new controller, no ownership change):
[x] Split into buildInterpolatedCamera, renderSceneAndOverlays, renderDeploymentGrabbedGhost, syncUnitPanelWindow, renderDeploymentHud, renderTopBattleText, renderEffects, renderUIStack, renderEndOverlay.
[x] Defeat/Victory overlay blocks deduplicated into one parameterized renderEndOverlay(bool victory) — same pixels, one code path instead of two.
[x] Flagged, not fixed: syncUnitPanelWindow() mutates state during the render pass (pre-existing behavior, preserved as-is — a real update/render timing smell worth a future look, not touched here).

BattleState itself is now down to: lifecycle (onEnter/onExit/handleInput/update/render), the turn-flow state machine (processCurrentTurn/advanceToNextUnit, deliberately NOT extracted — it's the conductor tying the four controllers together), and the Context-struct factories. This matches the "seams first, conductor stays" plan agreed at the start of the breakup.

=== UNIT REDESIGN / PROGRESSION SYSTEM (design only, still deferred) ===
[ ] Full Spanish-language design doc exists (not copied here) — recruited-army philosophy, three-layer unit identity, skill points, promotion system, equip-vs-learn separation. Confirmed isolated from BattleState/DeploymentSystem/BattleSession by existing boundaries.
[ ] Still explicitly deferred by user decision. BattleState breakup (the stated blocking condition) is now DONE — this is the natural next candidate once the user chooses to re-open it, but do NOT start unprompted.

=== STILL OPEN / NOT IMPLEMENTED ===
[x] Blurry text after resolution change — root cause diagnosed (SDL_SetRenderLogicalPresentation letterbox scaling likely defaults to linear filtering; Renderer::setTextureScaleMode is per-texture only, doesn't cover this). Needs real SDL3 documentation research before writing a fix — not yet done.
[ ] Turn queue UI widget — parked, no layout decided.
[ ] Real animations — blocked on assets.
[ ] Multi-cast skills ("Double Cast") — not implemented.
[ ] RosterSystem recruitment/dismissal UI — backend API exists, no UI.
[ ] World-map cities/nodes/NPC collisions — only WindowId/ActionId naming groundwork exists; no structs/design.
[ ] syncUnitPanelWindow() render/update timing smell (see Seam #4 note) — not urgent, flagged only.

=== NOTES FOR NEXT SESSION ===
BattleState breakup is DONE — all 4 seams extracted and compiling. Full deployment→combat→attack→victory/defeat smoke test still recommended after Seam #4, not yet explicitly confirmed by user.
Next real candidate for a large session: either the blurry-text SDL3 research, or re-opening the unit progression/class system now that its stated blocker (BattleState breakup) is resolved — user's call, don't assume.
The separate "Remaining Work — Handoff Plan" document is now RETIRED — its contents are fully absorbed into this summary (all of steps 0–4 are done or tracked above). Use this single document going forward.
Working-style notes (still apply): user tracks token/effort budget carefully; prefers real, correctly-generalized fixes over quick patches; always ask for current file contents before proposing edits to anything that may have changed; direct, code-first answers with minimal explanation once a design is agreed.